# Requirements untuk Production - Python 3.10 Compatible
# Website Desa Pulosarok - Production Ready
# Dikonfigurasi khusus untuk production dengan Python 3.10

# ==================== PYTHON VERSION REQUIREMENT ====================
# Python >= 3.10, < 3.12

# ==================== DJANGO CORE ====================
Django==4.2.16                    # Web framework utama - LTS version
django-environ==0.11.2           # Environment variables management
setuptools==80.9.0               # Package management

# ==================== PRODUCTION WSGI SERVER ====================
gunicorn==21.2.0                 # Production WSGI server
whitenoise==6.8.2               # Static file serving untuk production

# ==================== DATABASE DRIVERS ====================
psycopg2-binary==2.9.10          # PostgreSQL database adapter

# ==================== FORMS & UI ====================
django-crispy-forms==2.1         # Form rendering dengan Bootstrap
crispy-bootstrap5==0.7          # Bootstrap 5 integration
django-widget-tweaks==1.5.0     # Widget customization

# ==================== API & AUTHENTICATION ====================
djangorestframework==3.14.0     # REST API framework
djangorestframework-simplejwt==5.3.0  # JWT authentication
django-cors-headers==4.3.1      # CORS handling

# ==================== DATA MANAGEMENT ====================
django-import-export==3.3.1     # Import/Export data
pandas==2.2.3                   # Data analysis (Python 3.10 compatible)
numpy==1.26.4                   # Numerical computing (Python 3.10 compatible)

# ==================== FILE PROCESSING ====================
Pillow==10.4.0                  # Image processing (Python 3.10 stable)
python-docx==1.2.0              # Word document processing
reportlab==4.4.4                # PDF generation
qrcode==8.2                     # QR code generation
lxml==5.3.0                     # XML processing (Python 3.10 compatible)

# ==================== UTILITIES ====================
python-dotenv==1.0.0           # Environment management
django-extensions==3.2.3       # Django extensions
user-agents==2.2.0             # User agent parsing
colorama==0.4.6                # Terminal colors

# ==================== EXCEL & SPREADSHEET ====================
openpyxl==3.1.5                # Excel file handling (.xlsx)
xlsxwriter==3.2.9              # Excel writing dengan formatting (.xlsx)
xlrd==2.0.2                    # Excel reading (.xls)
xlwt==1.3.0                    # Excel writing (.xls)
odfpy==1.4.1                   # OpenDocument format (.ods)

# ==================== DATA EXPORT/IMPORT ====================
tablib==3.5.0                  # Data export/import
markuppy==1.18                 # HTML generation
defusedxml==0.7.1              # XML security
diff-match-patch==20241021     # Text diffing
et_xmlfile==2.0.0              # XML file handling

# ==================== SECURITY & VALIDATION ====================
# Production security packages
django-ratelimit==4.1.0        # Rate limiting
django-security==0.20.0        # Security middleware

# ==================== CACHE & SESSION ====================
django-redis==5.4.0            # Redis cache backend
redis==5.2.0                   # Redis client

# ==================== MONITORING & LOGGING ====================
sentry-sdk==2.19.0             # Error monitoring
django-structlog==4.0.0        # Structured logging

# ==================== SUPPORTING LIBRARIES ====================
asgiref==3.10.0                # ASGI support
sqlparse==0.5.3                # SQL parsing
tzdata==2025.2                 # Timezone data
pytz==2025.2                   # Timezone handling
PyJWT==2.10.1                  # JWT token handling
python-dateutil==2.9.0.post0   # Date utilities
six==1.17.0                    # Python 2/3 compatibility
charset-normalizer==3.4.3      # Character encoding
typing_extensions==4.15.0      # Type hints
ua-parser==1.0.1               # User agent parsing
ua-parser-builtins==0.18.0.post1  # User agent data
PyYAML==6.0.3                  # YAML processing

# ==================== TESTING & MONITORING ====================
requests==2.32.3               # HTTP requests
beautifulsoup4==4.12.3         # HTML parsing
psutil==6.1.0                  # System monitoring

# ==================== PRODUCTION OPTIMIZATION ====================
# Database connection pooling
django-db-pool==0.1.0          # Database connection pooling

# Static file optimization
django-compressor==4.4         # CSS/JS compression
django-minify==1.0.0           # HTML minification

# ==================== DEPLOYMENT NOTES ====================
# 1. Python Version: 3.10.x (recommended)
# 2. WSGI Server: Gunicorn dengan multiple workers
# 3. Static Files: WhiteNoise untuk serving
# 4. Database: PostgreSQL dengan connection pooling
# 5. Cache: Redis untuk session dan cache
# 6. Monitoring: Sentry untuk error tracking
# 7. Security: Rate limiting dan security middleware
# 8. Logging: Structured logging dengan django-structlog

# ==================== INSTALLATION COMMANDS ====================
# pip install -r requirements_production.txt
# python manage.py migrate
# python manage.py collectstatic --noinput
# python manage.py createsuperuser
# gunicorn --bind 0.0.0.0:8000 pulosarok_website.wsgi:application

# ==================== PRODUCTION DEPLOYMENT ====================
# 1. Set environment variables:
#    - DEBUG=False
#    - SECRET_KEY=your-secure-secret-key
#    - DATABASE_URL=postgresql://user:pass@host:port/db
#    - REDIS_URL=redis://localhost:6379/0
#    - SENTRY_DSN=your-sentry-dsn

# 2. Run migrations:
#    python manage.py migrate

# 3. Collect static files:
#    python manage.py collectstatic --noinput

# 4. Start production server:
#    gunicorn --workers 4 --bind 0.0.0.0:8000 pulosarok_website.wsgi:application

# ==================== PRODUCTION CHECKLIST ====================
# ✅ Python 3.10: Compatible
# ✅ Django 4.2.16: LTS Version
# ✅ PostgreSQL: psycopg2-binary
# ✅ Redis: Cache dan Session
# ✅ Gunicorn: WSGI Server
# ✅ WhiteNoise: Static Files
# ✅ Sentry: Error Monitoring
# ✅ Security: Rate limiting
# ✅ Logging: Structured
# ✅ Performance: Optimized
